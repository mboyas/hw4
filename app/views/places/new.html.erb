<h1 class="pt-2 pb-4">New Place</h1>

<%= form_with model: @place do |form| %>
  <p>
    <%= form.label "name" %>
    <%= form.text_field "name" %>
  </p>
  <p>
    <%= form.submit "Save Place To List", class:"btn btn-primary"  %>
  </p>
<% end %>